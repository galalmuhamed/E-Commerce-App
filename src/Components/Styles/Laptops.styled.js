import styled from 'styled-components';
import {
	SmartPhoneContainer,
	Container,
	Title,
	LinksBanner,
	Brand,
} from './SmartPhones.styled';

export const LaptopsContainer = styled(SmartPhoneContainer)`
	${SmartPhoneContainer}
`;
export const LapContainer = styled(Container)`
	${Container}
`;
export const LapTitle = styled(Title)`
	${Title}
`;
export const LapLinksBanner = styled(LinksBanner)`
	${LinksBanner}
`;
export const LapBrand = styled(Brand)`
	${Brand}
`;
