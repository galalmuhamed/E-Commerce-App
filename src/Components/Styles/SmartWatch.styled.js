import styled from 'styled-components';
import {
	SmartPhoneContainer,
	Container,
	Title,
	LinksBanner,
	Brand,
} from './SmartPhones.styled';

export const SmartWatchContainer = styled(SmartPhoneContainer)`
	${SmartPhoneContainer}
`;
export const WatchContainer = styled(Container)`
	${Container}
`;
export const WatchTitle = styled(Title)`
	${Title}
`;
export const WatchLinksBanner = styled(LinksBanner)`
	${LinksBanner}
`;
export const WatchBrand = styled(Brand)`
	${Brand}
`;
